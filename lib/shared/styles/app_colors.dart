import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color primary = Color(0xFF123456);
  static const Color secondary = Color(0xFF654321);
  static const Color background = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF222222);

  // Dark theme colors
  static const Color darkPrimary = Color(0xFF0A0A23);
  static const Color darkBackground = Color(0xFF181818);

  // Add more as needed
}